import React, { Component } from 'react'
import { Text, StyleSheet, View } from 'react-native'

export default class Mixte extends Component {
  render() {
    return (
      <View>
        <Text> Mixte </Text>
      </View>
    )
  }
}

const styles = StyleSheet.create({})
